アプリ概要

PC向けWebアプリで、リマインダーを登録・一覧表示・編集・削除し、指定日時にメール通知するアプリ

技術構成
フロント：HTML / CSS / JavaScript
バックエンド：Python / Flask
DB：PostgreSQL
メール送信：SMTP（Gmailなど）
定期実行：APScheduler または cron
開発環境：Docker / Docker Compose 推奨
主な機能

1. リマインダー登録

入力項目例：

タイトル
内容
通知日時
送信先メールアドレス
通知済みフラグ

例：

タイトル：薬を飲む
内容：夕食後の薬
通知日時：2026-05-21 20:00
送信先：example@gmail.com 2. リマインダー一覧表示

一覧画面で表示する項目：

タイトル
内容
通知日時
送信先メールアドレス
通知状況
編集ボタン
削除ボタン 3. リマインダー編集

登録済みのリマインダーについて、

タイトル変更
内容変更
通知日時変更
メールアドレス変更

ができるようにします。

4. メール通知

バックグラウンド処理で定期的にDBを確認します。

条件例：

通知日時 <= 現在時刻
かつ
通知済みフラグ = false

該当するリマインダーがあればメール送信し、送信後に notified = true に更新します。

DBテーブル例
CREATE TABLE reminders (
id bigserial PRIMARY KEY,
title varchar(100) NOT NULL,
body text,
remind_at timestamptz NOT NULL,
email varchar(255) NOT NULL,
notified boolean NOT NULL DEFAULT false,
created_at timestamptz NOT NULL,
updated_at timestamptz NOT NULL
);
画面構成
/reminders
リマインダー一覧

/reminders/new
新規登録画面

/reminders/<id>/edit
編集画面

/reminders/<id>/delete
削除処理
API寄りにするなら
GET /reminders
POST /reminders
GET /reminders/<id>
PUT /reminders/<id>
DELETE /reminders/<id>
個人開発の実装順

まずはこの順番が作りやすいです。

リマインダー登録
一覧表示
編集
削除
メール送信処理
定期実行処理
通知済み管理
バリデーション
ログイン機能

最初はログインなしで作ってOKです。
完成後にユーザー管理を追加すると、かなり実践的なアプリになります。
